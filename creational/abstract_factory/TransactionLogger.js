class TransactionLogger {
    logTransaction(transactionDetails) {
        throw new Error("Method 'logTransaction(transactionDetails)' must be implemented.");
    }
}

module.exports = TransactionLogger;
